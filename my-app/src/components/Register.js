import React from 'react';

class Register extends React.Component {
  render () {
    return (
             <button className={"RegisterButton"} type={"submit"}>Załóż konto</button>  
    )
  };
  
};

export default Register;